'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.createStore = undefined;

var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol ? "symbol" : typeof obj; };

var _redux = require('redux');

var _reducer = require('./reducer');

var factory = function factory(initialState) {
  if (process.env.NODE_ENV === 'production') {
    return (0, _redux.createStore)(_reducer.componentRouter, initialState);
  }

  var devTools = _typeof(global.window) === 'object' && typeof global.window.devToolsExtension !== 'undefined' && global.window.devToolsExtension;

  if (!devTools) {
    return (0, _redux.createStore)(_reducer.componentRouter, initialState);
  }

  return (0, _redux.createStore)(_reducer.componentRouter, initialState, devTools());
};

var createStore = exports.createStore = factory;
//# sourceMappingURL=store.js.map