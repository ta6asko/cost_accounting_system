'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.renderRoute = undefined;

var _parse = require('./parse');

var _invariant = require('fbjs/lib/invariant');

var _invariant2 = _interopRequireDefault(_invariant);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var renderRoute = exports.renderRoute = function renderRoute() {
  var route = arguments.length <= 0 || arguments[0] === undefined ? '/' : arguments[0];
  return function () {
    var params = arguments.length <= 0 || arguments[0] === undefined ? {} : arguments[0];

    var parts = route.match(_parse.routePartsRegex);

    if (!parts || !parts.length) {
      return '/';
    }

    return parts.reduce(function (pathname, part) {
      if (part.substr(0, 1) !== ':') {
        return pathname + '/' + encodeURIComponent(part);
      }

      var name = part.substr(1);

      if (process.env.NODE_ENV !== 'production') {
        (0, _invariant2.default)(params.hasOwnProperty(name), 'Param :' + name + ' is not specified for route ' + route);
      }

      return pathname + '/' + encodeURIComponent(params[name]);
    }, '');
  };
};
//# sourceMappingURL=render.js.map